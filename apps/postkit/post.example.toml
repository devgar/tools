# Example post file. Usage: postkit compose post.example.toml

text     = "Testing postkit, a multi-platform scheduler in Rust 🦀. Code at https://github.com/devgar/tools"
hashtags = ["rust", "opensource"]

# Uncomment to attach images:
# [[media]]
# path = "/path/to/image.png"
# alt  = "Terminal showing the output of postkit verify"

# Optional per-platform overrides.
# Each section overrides only the specified fields for that platform.
# Valid keys: bluesky, x, meta_page, meta_instagram.

# [platforms.x]
# text = "Short version for X (max 280 chars) 🦀 https://github.com/devgar/tools"

# [platforms.bluesky]
# text     = "Longer version for Bluesky with more context."
# hashtags = ["rust", "bluesky", "opensource"]
